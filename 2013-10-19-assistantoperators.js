Assignment Operators:

An assignment operator assigns a value to its left operannd based on the value of its right operands.
The basic assignment operator is equal (=), which assigns the value of its right operand to its left operand.
That is, x = y assigns the value of y to x. The other assignments operators are shorthands for operations listed
in the following table:

Operator:               Meaning:
x += y
x -= y
x *= y
x /= y
x %= y
x <<= y
x >>= y
